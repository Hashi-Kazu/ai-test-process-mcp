import type { ThresholdExtractionCriteria } from "../types.js";

// reexpand_threshold_changes の「文書からの閾値自前抽出」判定区分カタログ。
// 自作のパラフレーズであり、原典の逐語転載はしない。
export const thresholdExtractionCriteria: ThresholdExtractionCriteria = {
  name: "閾値自前抽出 判定区分カタログ",
  summary:
    "投入された変更前後の仕様書テキストから閾値パラメータ候補を決定的に抽出し、呼び出し側が宣言したパラメータ表と突合したときに現れる不一致の種類を分類するための判定区分。",
  categories: [
    {
      id: "TCE-01",
      nameJa: "未宣言の閾値候補",
      severity: "medium",
      description: "文書から抽出した閾値候補に対応する宣言パラメータが存在しない。",
      action:
        "宣言漏れか対象外かを判断し、対象なら parametersBefore / parametersAfter へ追加して再実行すること。",
    },
    {
      id: "TCE-02",
      nameJa: "宣言値と文書値の不一致",
      severity: "high",
      description: "宣言パラメータと対応づいた文書側の候補で、値または単位が食い違っている。",
      action: "宣言表と仕様書のどちらが正かを確定し、正しい値へ揃えてから再展開すること。",
    },
    {
      id: "TCE-03",
      nameJa: "文書に裏付けの無い宣言パラメータ",
      severity: "medium",
      description:
        "宣言パラメータに対応する候補が抽出されず、同値・同単位の数値表現も文書中に見つからない。",
      action:
        "当該パラメータの根拠となる文書を投入するか、根拠が無いなら宣言表から除くか出典を明示すること。",
    },
    {
      id: "TCE-04",
      nameJa: "文書差分と宣言差分の不整合",
      severity: "high",
      description:
        "文書から抽出した新旧差分と、宣言パラメータ表の新旧差分が一致していない（片側だけ変更されている）。",
      action: "宣言表への反映漏れか、抽出の取りこぼしかを確認し、双方の差分を一致させること。",
    },
    {
      id: "TCE-05",
      nameJa: "文書内での値の衝突",
      severity: "medium",
      description:
        "同一スナップショット内の同じラベルに対し、異なる値または単位が複数箇所で記載されている。",
      action: "仕様書側の記載を統一し、どの箇所が正であるかを確定してから再実行すること。",
    },
    {
      id: "TCE-06",
      nameJa: "抽出候補に存在しない承認",
      severity: "high",
      description:
        "approvedExtractions に指定された名前の候補が抽出されていない、または承認値が候補の値と一致しない。",
      action:
        "承認内容を抽出結果の新旧対照表に合わせて修正すること。一致しない承認は再展開へ反映されない。",
    },
    {
      id: "TCE-07",
      nameJa: "未承認の抽出候補",
      severity: "info",
      description: "抽出した新旧対照表の行が承認されておらず、再展開へ反映されていない。",
      action:
        "新旧対照表を確認し、採用する候補を approvedExtractions へ渡して再実行すること。",
    },
  ],
  notes: [
    "自前抽出は投入された文書テキストのみを対象とする。抜粋を投入した場合、抽出結果も抜粋の範囲に限定される。",
    "抽出結果は再展開へ自動反映されない。人が承認した候補だけが実効パラメータ表へ追記され、宣言済みの値は決して上書きされない。",
    "抽出は表形式・ラベル付き行という決定的な書式に依存する。散文中に埋もれた閾値は候補にならないため、未宣言候補が0件でも宣言漏れが無いことの証明にはならない。",
    "本カタログの判定はすべて決定的であり、件数のみを提示する。網羅率・カバー率のような達成度の割合は算出しない。",
  ],
};
